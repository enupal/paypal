{#
/**
 * EnupalPaypal plugin for Craft CMS 3.x
 *
 * @link      https://enupal.com/
 * @copyright Copyright (c) 2018 Enupal
 */
#}

{% extends "enupal-paypal/_layouts/settings" %}
{% import "_includes/forms" as forms %}

{% set settings = craft.enupalbackup.getSettings() %}
{% set fullPageForm = true %}
{% set saveShortcutRedirect = 'enupal-paypal/settings/objectStorage' %}

{% set saveButton %}
	<div class="buttons">
		<input class="btn submit" type="submit" value="{{ 'Save'|t('enupal-paypal') }}">
	</div>
{% endset %}

{% set content %}

	<input type="hidden" name="action" value="enupal-paypal/settings/save-settings">
	<input type="hidden" name="backupScenario" value="sos">

	<div id="settings-cp">

		{% namespace 'settings' %}

			{{ forms.lightswitchField({
				label: "Enable Softlayer Object Storage"|t('enupal-paypal'),
				id: 'enableSos',
				name: 'enableSos',
				on: settings.enableSos,
				onLabel: "On"|t('enupal-paypal'),
				offLabel: "Off"|t('enupal-paypal'),
				errors: settings.getErrors('enableSos'),
			}) }}

			{{ forms.textField({
				label: "User"|t('enupal-paypal'),
				id: 'sosUser',
				name: 'sosUser',
				instructions: "The user you want to connect with."|t('enupal-paypal'),
				value: settings.sosUser,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Secret"|t('enupal-paypal'),
				id: 'sosSecret',
				name: 'sosSecret',
				instructions: "The api key to authenticate the user."|t('enupal-paypal'),
				value: settings.sosSecret,
				required: true,
				type: "password",
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Host"|t('enupal-paypal'),
				id: 'sosHost',
				name: 'sosHost',
				instructions: "The host you want do copy your backups to."|t('enupal-paypal'),
				value: settings.sosHost,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Container"|t('enupal-paypal'),
				id: 'sosContainer',
				name: 'sosContainer',
				instructions: "The Object Storage Container where to put the backup."|t('enupal-paypal'),
				value: settings.sosContainer,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Path"|t('enupal-paypal'),
				id: 'sosPath',
				name: 'sosPath',
				instructions: "The remote path where to copy the backup."|t('enupal-paypal'),
				value: settings.sosPath,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

		{% endnamespace %}

	</div>

{% endset %}