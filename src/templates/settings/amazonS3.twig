{#
/**
 * EnupalPaypal plugin for Craft CMS 3.x
 *
 * @link      https://enupal.com/
 * @copyright Copyright (c) 2018 Enupal
 */
#}

{% extends "enupal-paypal/_layouts/settings" %}
{% import "_includes/forms" as forms %}

{% set settings = craft.enupalbackup.getSettings() %}
{% set fullPageForm = true %}
{% set saveShortcutRedirect = 'enupal-paypal/settings/amazonS3' %}

{% set saveButton %}
	<div class="buttons">
		<input class="btn submit" type="submit" value="{{ 'Save'|t('enupal-paypal') }}">
	</div>
{% endset %}

{% set content %}

	<input type="hidden" name="action" value="enupal-paypal/settings/save-settings">
	<input type="hidden" name="backupScenario" value="amazon">

	<div id="settings-cp">

		{% namespace 'settings' %}

			{{ forms.lightswitchField({
				label: "Enable Amazon"|t('enupal-paypal'),
				id: 'enableAmazon',
				name: 'enableAmazon',
				on: settings.enableAmazon,
				onLabel: "On"|t('enupal-paypal'),
				offLabel: "Off"|t('enupal-paypal'),
				errors: settings.getErrors('enableAmazon'),
			}) }}

			{{ forms.textField({
				label: "Key"|t('enupal-paypal'),
				id: 'amazonKey',
				name: 'amazonKey',
				instructions: "The amazon S3 key."|t('enupal-paypal'),
				value: settings.amazonKey,
				required: true,
				placeholder: "xxxxxxx"|t('enupal-paypal'),
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Secret"|t('enupal-paypal'),
				id: 'amazonSecret',
				name: 'amazonSecret',
                type: 'password',
				instructions: "The amazon S3 secret."|t('enupal-paypal'),
				value: settings.amazonSecret,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Bucket"|t('enupal-paypal'),
				id: 'amazonBucket',
				name: 'amazonBucket',
				instructions: "A bucket where to store the backup."|t('enupal-paypal'),
				value: settings.amazonBucket,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Region"|t('enupal-paypal'),
				id: 'amazonRegion',
				name: 'amazonRegion',
				instructions: "The region where the bucket is located e.g. 'eu-central-1'."|t('enupal-paypal'),
				value: settings.amazonRegion,
				required: true,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.textField({
				label: "Path"|t('enupal-paypal'),
				id: 'amazonPath',
				name: 'amazonPath',
				instructions: "Path where to store the backup in your bucket."|t('enupal-paypal'),
				value: settings.amazonPath,
				autofocus: true,
				first: true,
				errors: ''
			}) }}

			{{ forms.lightswitchField({
				label: "MultiPartUpload functionality"|t('enupal-paypal'),
				instructions: "Use Amazon S3 MultiPartUpload functionality."|t('enupal-paypal'),
				id: 'amazonUseMultiPartUpload',
				name: 'amazonUseMultiPartUpload',
				on: settings.amazonUseMultiPartUpload,
				onLabel: "On"|t('enupal-paypal'),
				offLabel: "Off"|t('enupal-paypal')
			}) }}

		{% endnamespace %}

	</div>

{% endset %}